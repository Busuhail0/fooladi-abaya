# نشر فولادي على Cloudflare وربطه بـGitHub

**البرنامج غير منشور بعد.** هذه حزمة مستقلة مخصصة للسحابة، وتحتفظ بلغة بايثون. تشغيلها يحتاج مستودع GitHub وحساب Cloudflare يمكنه إنشاء Worker وقاعدة D1 وحاوية R2 خاصة.

## ١. مستودع GitHub

أنشئ مستودعاً **خاصاً** باسم `fooladi-abaya`، وأتحه لاتصال GitHub المستخدم لإدارة المشروع. ارفع محتويات هذا المجلد إلى جذر المستودع، بما فيها مجلد `.github` وملف `.gitignore`.

لا ترفع بيانات العملاء أو النسخ الاحتياطية أو `.dev.vars` أو ملفات المفاتيح. قاعدة البيانات والصور الفعلية تحفظ في Cloudflare، وليست في GitHub. قائمة الاستبعاد مضمنة في `.gitignore`.

## ٢. أدوات النشر

على جهاز مسؤول النشر يلزم Python 3.12+ وNode.js و[uv](https://docs.astral.sh/uv/). من مجلد المشروع:

```bash
uv sync
uv run pywrangler login
python scripts/bundle_assets.py
```

إتمام تسجيل الدخول يكون مع Cloudflare مباشرة. لا ترسل كلمة مرور حسابك أو رمز API في المحادثة.

## ٣. إنشاء قاعدة البيانات والصور

```bash
uv run pywrangler d1 create fooladi-abaya-db
uv run pywrangler r2 bucket create fooladi-abaya-models
```

انسخ `database_id` الفعلي الذي تعيده Cloudflare إلى ملف `wrangler.jsonc` مكان `REPLACE_WITH_CREATED_D1_DATABASE_ID`. لا تعدل أسماء الربط `DB` و`PHOTOS`.

بديل تعديل الملف: عيّن متغير البيئة `FOOLADI_D1_DATABASE_ID` للقيمة الفعلية ثم شغّل:

```bash
python scripts/configure_d1.py
```

اترك حاوية R2 **خاصة**؛ لا تفعّل public access أو `r2.dev`. البرنامج يعرض الصورة للمستخدم المسجّل فقط. إذا طلب الحساب تفعيل R2 أو إضافة وسيلة دفع، أكمل ذلك من حسابك بعد مراجعة الخطة؛ الحزمة لا تفعّل اشتراكاً أو خطة مدفوعة تلقائياً.

## ٤. أسرار التشغيل وحساب المالك

أنشئ قيمتين عشوائيتين مستقلتين وطويلتين في مدير كلمات مرور موثوق:

- `SESSION_SECRET`: توقيع جلسات الدخول، 32 حرفاً على الأقل، ويفضل 64 حرفاً عشوائياً.
- `SETUP_TOKEN`: رمز سري مختلف لتهيئة حساب المالك للمرة الأولى.

سجلهما كـWorkers secrets، وليسا متغيرات عادية ولا ملفات في GitHub:

```bash
uv run pywrangler secret put SESSION_SECRET
uv run pywrangler secret put SETUP_TOKEN
```

اسم الـWorker هو `fooladi-abaya`. إذا طلبت الأداة إنشاء Worker عند إدخال أول سر، أنشئه بالاسم نفسه. لا تختَر مشروعاً قائماً غير تابع لفولادي.

عند أول فتح للبرنامج ستحتاج رمز تهيئة المالك لإنشاء اسم المستخدم وكلمة المرور. بعد إنشاء الحساب يغلق مسار التهيئة. تحتفظ بكلمة المرور والرمز بنفسك؛ لا توجد بيانات دخول افتراضية.

للتجربة المحلية فقط، يمكن تشغيل `python scripts/create_local_secrets.py` لإنشاء `.dev.vars`. لا تستخدم قيم المثال الموجودة في `.dev.vars.example` في الإنتاج.

## ٥. الفحص والنشر الأول

```bash
python scripts/bundle_assets.py
uv run pytest -q
uv run pywrangler deploy --dry-run
uv run pywrangler d1 migrations apply fooladi-abaya-db --remote
uv run pywrangler deploy
```

ابدأ بموارد تجريبية مستقلة إذا كانت هناك بيانات فعلية. لم يكتمل `deploy --dry-run` أثناء تجهيز هذه الحزمة لأن تنزيل Pyodide من GitHub انتهى بمهلة؛ لذلك لا تتجاوز هذا الفحص على جهاز النشر أو في GitHub Actions.

الأداة تعيد رابط التشغيل الفعلي. لا تعتمد على تخمين رابط `workers.dev` من اسم المشروع؛ يختلف الجزء الخاص بالحساب. يمكن ربط نطاق خاص من إعدادات Cloudflare لاحقاً.

## ٦. الربط بالنشر من GitHub

توجد طريقتان؛ استخدم واحدة للنشر حتى لا يعمل مساران في الوقت نفسه.

### الطريقة المضمنة: GitHub Actions

من Settings → Secrets and variables → Actions، ضع:

| النوع | الاسم | القيمة |
|---|---|---|
| Secret | `CLOUDFLARE_API_TOKEN` | رمز نشر محدود بصلاحيات هذا المشروع |
| Secret | `CLOUDFLARE_ACCOUNT_ID` | معرّف حساب Cloudflare |
| Variable | `FOOLADI_D1_DATABASE_ID` | المعرّف الفعلي لقاعدة D1 |

يبقى `SESSION_SECRET` و`SETUP_TOKEN` في Workers secrets؛ لا يحتاج سير العمل إلى كشفهما.

يوجد سير تحقق تلقائي باسم **Validate Fooladi** عند الدفع إلى `main` أو فتح طلب دمج. وسير نشر يدوي باسم **Deploy Fooladi to Cloudflare** ضمن Actions. النشر يستخدم بيئة `production`، وينفذ الاختبارات وفحص البناء وترقيات القاعدة ثم النشر. ابدأ به بعد إنشاء الموارد والأسرار.

للنشر التلقائي لاحقاً، يمكن ربط Worker مباشرة بالمستودع، أو تعديل شروط سير النشر بعد نجاح التجربة. لا يوجد نشر تلقائي عند كل تعديل في هذه الحزمة الافتراضية.

### بديل: Cloudflare Workers Builds

اربط مستودع GitHub من إعدادات Worker. أمر البناء:

```bash
python -m pip install uv
uv sync
python scripts/bundle_assets.py
uv run pytest -q
```

أمر النشر: `uv run pywrangler deploy`. طبّق ترقيات D1 المنصوص عليها أعلاه قبل نشر إصدار يعتمد عليها، أو ضمّنها في عملية نشر مضبوطة. استخدم معرّف D1 الحقيقي في الملف؛ لا تترك النص البديل.

دليل الربط الرسمي: [GitHub integration](https://developers.cloudflare.com/workers/ci-cd/builds/git-integration/github-integration/).

## ٧. اختبار التشغيل قبل البيانات الفعلية

تحقق في النسخة التجريبية من: إنشاء حساب المالك بالرمز، الدخول والخروج، رفع صورة موديل، إدخال رقمه في طلب وظهور الصورة، حفظ أكثر من عباية، تحديث جميع المراحل، تسجيل عربون والدفعة الأخيرة، الاستلام والتوصيل، معاينة طباعة الفاتورة، وتصدير نسخة احتياطية.

جرّب فتح جلسة ثانية وتحديث الطلب بالتزامن؛ قد يطلب البرنامج تحديث الصفحة وإعادة الحفظ عند تعارض تعديلين، وهو سلوك مقصود لمنع فقدان البيانات. تحقق أيضاً من أن رابط الصورة لا يفتح دون تسجيل الدخول.

قيّم زمن تسجيل الدخول ورفع الصورة حسب حدود CPU والذاكرة لخطة Workers. Python Workers موصوفة حالياً بأنها beta في وثائق Cloudflare. لا تُعتبر اختبارات Python المحلية بديلاً عن اختبار التشغيل الفعلي.

## ٨. النسخ الاحتياطي والاستعادة

زر النسخ الاحتياطي يجمع لقطة متسقة من جداول D1 وصور الموديلات المشار إليها في الطلبات داخل ZIP، بحد 10,000 صف لكل جدول، و100 صورة، وإجمالي بيانات غير مضغوطة 20 MiB. للبيانات الأكبر استخدم تصدير D1 ونسخ R2 من أدوات الإدارة. لم يُضبط نسخ احتياطي مجدول تلقائياً.

تحضير نسخة سحابية للاستعادة:

```bash
python scripts/prepare_restore.py path/to/fooladi-cloud-backup.zip
```

ينتج مجلد `restore-output` وفيه `database.sql` و`photos/`. هذا التحضير محلي فقط ولا يغير السحابة.

للاستعادة الآمنة: أنشئ قاعدة D1 **جديدة وفارغة** وحاوية R2 **جديدة وخاصة**. طبّق ملف `migrations/0001_initial.sql` على القاعدة الجديدة، ثم استورد ملف `database.sql` باستخدام أمر `d1 execute` مع `--remote --file`. ارفع كل صورة من `photos/` إلى المسار `models/اسم-الصورة.jpg` في الحاوية الجديدة. استخدم `r2 object put` مع `--remote` و`--file`، وفق أسماء الموارد الجديدة.

بعد التحقق من عدد الطلبات والمدفوعات والصور، بدّل ربط DB وPHOTOS في إعدادات النشر إلى الموارد الجديدة. احتفظ بالموارد الأصلية حتى التأكد من الاستعادة، ولا تستورد الملف فوق قاعدة قائمة. ستستخدم بيانات دخول حساب النسخة المستعادة.

لأعمال الصيانة يمكن ضبط `MAINTENANCE` إلى `1` ثم نشر الإعداد؛ يعرض التطبيق رسالة صيانة ولا يستقبل تغييرات. أعده إلى `0` بعد الانتهاء.

## ٩. المتبقي لإكمال الربط في هذه المحادثة

- تفعيل اتصال Cloudflare بالحساب المقصود.
- مستودع المشروع هو `Busuhail0/fooladi-abaya`؛ تحقق من إعداد الخصوصية المطلوب للحساب.
- استكمال فحص بناء Workers والتجربة الفعلية قبل اعتماد رابط التشغيل.

رفع الكود إلى GitHub لا ينشر البرنامج تلقائياً. لم تُنشأ موارد Cloudflare ولم يصدر رابط تشغيل من هذا المشروع حتى الآن.
